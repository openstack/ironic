---
fixes:
  - |
    Firmware components are now also cached on the transition to the
    ``manageable`` state in addition to cleaning. This is consistent with how
    BIOS settings, vendor and boot mode are cached.
