---
fixes:
  - Fixes `an issue <https://bugs.launchpad.net/ironic/+bug/1712359>`_ where
    the update of a MAC address failed for ports that were bound (for example,
    when using the 'contrail' neutron backend).
