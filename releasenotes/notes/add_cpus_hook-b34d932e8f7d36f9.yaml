---
features:
  - |
    Implements `cpus` inspection hook which used to be part of the standard
    hooks in the ironic-inpsector.
fixes:
  - |
    Updates slightly incorrect docs for Migration from Inspector.
    `local_gb` hook is supported, but it is in `root-device` hook.
